package Zuhause;

import java.io.IOException;

import oop.IO;
import Runden.Runden;

public class Info {

	public static void main(String[] args) {
		// < kleiner als
		// > größer als
		// <= kleiner gleich
		// >= größer gleich
		// == gleich 
		// != ungleich
		
		// && und 				Beide Ausdrücke müssen gleich sein
		// || oder				Mindestens einer der Ausdrücke muss wahr sein
		// ^  exklusives oder 	Genau einer der beiden Ausdrücke ist wahr
		
		// int b; Datentyp, Name == Deklaration (Vergeben eines NamenS)
		// b = 3; Variable, optionale Wertzuweisung == Variable wurde Initialisiert
		
		// Immer CamelCase benutzen also zusammen schreiben und neues Wort groß
		
		// Char=		Ein Buchstabe, eine Zahl oder ein Sonderzeichen. 
		// 				Eingabe des Zeichens in einfachen Anführungszeichen ('H').
		// String= 		Eine Zeichenkette bzw. ein Text bestehend aus Buchstaben, 
		// 				Zahlen und Zeichen. Eingabe des Textes in doppelten Anführungszeichen ("Hallo")...
		// boolean= 	wahr/falsch
		// Ganzzahlen=	int
		// Kommazahlen= double 
		
		// Auf zwei nachkommastellen runden Rn = Rn * 100;
										//	Rn = Math.round(Rn);
										//	Rn = Rn / 100;
		
		// Oder Runden: Runden.Runden.runden(Stelle vor dem Komma welches Ergebnis soll da stehe, kommastellen)
		// 				Package.class.methode
		
		// System.out.println("\t"); = Tabulator setzen
		// System.out.println("\n"); = Erst in nächster Zeile wieder anfangen
		
		//double kommazahl = Double.parseDouble(text.trim().replace(',', '.'));
		//int ganzzahl = Integer.parseInt(br.readLine());
			
	}
}
